/** AuthKit callback path — must match WorkOS redirect URI and NEXT_PUBLIC_WORKOS_REDIRECT_URI. */
export const AUTH_CALLBACK_PATH = "/callback";

/** App sign-in endpoint — must match WorkOS initiate login URI. */
export const AUTH_LOGIN_PATH = "/login";

/** App sign-out endpoint. */
export const AUTH_LOGOUT_PATH = "/logout";

/** Default post-auth / post-logout landing. */
export const AUTH_RETURN_HOME = "/";
